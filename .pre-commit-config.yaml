repos:
  - repo: local
    hooks:
      - id: run-pytests
        name: Run pytest via runtest.py (use repo venv when available)
        entry: python tools/run_tests_wrapper.py -q
        language: system
        pass_filenames: false
        env:
          OPENAI_API_KEY: $OPENAI_API_KEY