# book_reader
Project Purpose: Local, CPU-based book summarizer that reads a PDF, performs multi-pass LLM summarization (chunk → chunk summaries → combined final summary), and exposes a Gradio web UI for running summarization and generating presentation text.
